# trying-angular

trying things in angular
npm install -D @testing-library/angular@16  
npm install -D vitest @vitest/browser-preview
npm i -D @vitest/browser-playwright
npm install -D vitest jsdom @vitest/ui
npm install -D happy-dom
